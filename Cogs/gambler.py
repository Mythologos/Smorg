import discord
from discord.ext import commands


class Gambler(commands.Cog):
    ...
    # TODO: define class, set up roll types and syntax (regular, blind)
    # Current Plans: regular dice (assume d6 as default). Allow for xdy rolls; build in possibility for keep / drop.

